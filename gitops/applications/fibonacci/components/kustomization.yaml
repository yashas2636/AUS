apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml

configMapGenerator:
  - name: newrelic-config
    files:
      - newrelic.yml
    options:
      disableNameSuffixHash: true
